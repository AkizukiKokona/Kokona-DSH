* text=auto eol=lf
*.png binary
*.jpg binary
*.ico binary
# cmd.exe mis-parses LF-only batch files (labels and goto in particular), so a
# fresh clone has to get CRLF back even though the repo stores LF.
*.cmd text eol=crlf
*.bat text eol=crlf
